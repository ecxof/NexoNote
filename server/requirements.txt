# Every Python dependency NexoNote uses.
#   pip install -r server/requirements.txt
#
# Split so the FastAPI backend can be skipped: semantic linking works on its own,
# which is what `npm run setup:python -- --no-backend` installs.
-r semantic/requirements.txt
-r api/requirements.txt
