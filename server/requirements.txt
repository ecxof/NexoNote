# Every Python dependency NexoNote uses.
#   pip install -r server/requirements.txt
#
# Only the FastAPI backend needs Python now. Semantic linking runs in the
# app itself; see src/features/semantic/tfidfPipeline.js.
-r api/requirements.txt
